<%= form_for(@locator) do |f| %>
  <% if @locator.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@locator.errors.count, "error") %> prohibited this locator from being saved:</h2>

      <ul>
      <% @locator.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :css %><br>
    <%= f.text_area :css %>
  </div>
  <div class="field">
    <%= f.label :brand %><br>
    <%= f.text_area :brand %>
  </div>
  <div class="field">
    <%= f.label :step %><br>
    <%= f.text_area :step %>
  </div>
  <div class="field">
    <%= f.label :offer %><br>
    <%= f.text_area :offer %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
