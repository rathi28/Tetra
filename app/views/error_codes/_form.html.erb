<%= form_for(@error_code) do |f| %>
    <% if @error_code.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@error_code.errors.count, "error") %> prohibited this error_code from being saved:</h2>

        <ul>
        <% @error_code.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <div class="form-group field">
      <%= f.label :human_name %><br>
      <%= f.text_field :human_name, class: 'form-control' %>
    </div>
    <div class="form-group field">
      <%= f.label :errorcode %><br>
      <%= f.number_field :errorcode, class: 'form-control' %>
    </div>
    <div class="actions">
      <%= f.submit class: 'btn btn-success' %>
    </div>

<% end %>
